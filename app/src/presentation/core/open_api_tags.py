from enum import Enum

class OpenApiTags(Enum):
    tag = "tag"
    user = "user"
    tool = "tool"
    room = "room"
    auth = "auth"
    event = "event"
    event_room = "event_room"
    map = "map"
    weather = "weather"
